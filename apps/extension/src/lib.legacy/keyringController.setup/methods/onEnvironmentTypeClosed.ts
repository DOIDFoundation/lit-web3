import { POLLING_TOKEN_ENVIRONMENT_TYPES } from '~/constants/app'

export const onEnvironmentTypeClosed = function (environmentType: string) {
  // const appStatePollingTokenType = POLLING_TOKEN_ENVIRONMENT_TYPES[environmentType]
  // const pollingTokensToDisconnect = this.appStateController.store.getState()[appStatePollingTokenType]
  // pollingTokensToDisconnect.forEach((pollingToken) => {
  //   this.gasFeeController.disconnectPoller(pollingToken)
  //   this.appStateController.removePollingToken(pollingToken, appStatePollingTokenType)
  // })
}
